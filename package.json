{
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky",
    "lint": "npm --prefix hackathon_frontend run lint",
    "lint:fix": "npm --prefix hackathon_frontend run lint -- --fix",
    "test:frontend": "npm --prefix hackathon_frontend test",
    "test:backend": "cd hackathon_backend && uv run pytest -q"
  }
}
